#ifndef ALGO_UTILS_H_
#define ALGO_UTILS_H_

#include "Base.h"

float rnd(float min = 0.0, float max = 1.0);
int rndIndex(int minimum = 0, int maximum = 1);
float rndSign();
#define SQR(x) ((x) * (x))

#endif